{
  "name": "react-qiniu-example",
  "private": true,
  "version": "0.0.0",
  "author": "Yuan He",
  "license": "MIT",
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  },
  "scripts": {
    "prepublish": "cd .. && npm install react && cd - && browserify --debug app.js > script.js"
  },
  "devDependencies": {
    "babelify": "^10.0.0",
    "browserify": "^16.5.0"
  }
}
